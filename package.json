{
  "name": "ebs-ediya-barista",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "EBS 이디야 커피 알바생이 되어 손님 주문대로 음료를 만드는 게임 (SQLite + Claude API)",
  "scripts": {
    "start": "node --env-file-if-exists=.env --env-file-if-exists=.env.local server.js",
    "dev": "node --watch --env-file-if-exists=.env --env-file-if-exists=.env.local server.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "express": "^5.2.1",
    "openai": "^7.15.0"
  }
}
